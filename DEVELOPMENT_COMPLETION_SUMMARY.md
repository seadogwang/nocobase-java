# NocoBase Java 后端迁移 — 开发完成总结

> 生成日期: 2026-08-29  
> 测试命令: `mvn test`  
> 测试结果: **41 tests, 0 failures, 0 errors, BUILD SUCCESS**

---

## 一、总体进度

| 批次 | 任务 | 状态 | 说明 |
|------|------|------|------|
| **Batch 1** | A0 项目基线治理 | ✅ 完成 | Flyway 迁移, ddl-auto=none, 测试基础设施 |
| | A1 API 兼容基线 | ✅ 完成 | 统一响应封装, 全局异常处理, 6 个 API 集成测试 |
| | B1 Collection 元数据模型 | ✅ 完成 | 完整字段模型, 支持 physical/view/sql/external |
| | B2 Field 元数据模型 | ✅ 完成 | 关系字段, 字段名校验, 物理列标记 |
| **Batch 2** | C1 Collection Runtime Registry | ✅ 完成 | 启动加载, reloadAll/reload, 线程安全 |
| | C2 Collection Capability 模型 | ✅ 完成 | 按类型区分可读/可写/可改结构 |
| | E1 DDL Synchronizer | ✅ 完成 | 建表/加字段/删字段, 方言适配器 |
| | E2 字段类型映射 | ✅ 完成 | 16 种字段类型映射, 标量/关系分类 |
| **Batch 3** | D1 DynamicRepository CRUD | ✅ 完成 | 元数据驱动, 替换原始 JDBC |
| | D2 Filter DSL 编译器 | ✅ 完成 | 13 种操作符, 嵌套 $and/$or, 参数绑定 |
| | D3 排序/分页/字段投影 | ✅ 完成 | sort, page/pageSize, fields 投影 |
| **Batch 4** | G1 ACL 数据模型 | ✅ 完成 | 4 个权限实体 + Repository |
| | F1 关系字段查询 | ⏳ 待完善 | appends 机制预留 |
| | F2 关联动作 | ⏳ 待完善 | 关联资源动作预留 |
| | G2 ACL Query 注入 | ⏳ 待完善 | AclService 预留 |
| | H1 系统模块注册表 | ⏳ 待完善 | builtIn 标记已实现 |
| | H2 UI Schema Storage | ⏳ 待完善 | 基础接口已存在 |

---

## 二、已完成的 12 个核心任务详情

### A0: 项目基线治理

**主要改动文件:**
- `src/main/resources/application.yml` — 禁用 ddl-auto, 启用 Flyway
- `src/main/resources/db/migration/V1__baseline_schema.sql` — 14 张表的 baseline 迁移
- `src/test/resources/application.yml` — 测试用 H2 内存数据库
- `src/test/java/com/nocobase/NocobaseApplicationTests.java` — 上下文加载测试

**设计说明:**
- 使用 Flyway 替代 Hibernate ddl-auto, 确保数据库结构可追溯
- 数据表以 PostgreSQL 为目标设计, H2 用于开发测试
- 使用 `GENERATED BY DEFAULT AS IDENTITY` 兼容 H2 和 PostgreSQL
- 所有标识符使用双引号保护, 配合 `globally_quoted_identifiers=true`

**测试命令:** `mvn test -Dtest=NocobaseApplicationTests`  
**测试结果:** 1 test passed  
**是否修改前端文件:** 否

---

### A1: API 兼容基线

**主要改动文件:**
- `src/main/java/com/nocobase/web/ApiResponse.java` — 统一响应封装
- `src/main/java/com/nocobase/web/GlobalExceptionHandler.java` — 全局异常处理
- `src/main/java/com/nocobase/web/ResourceNotFoundException.java` — 标准异常
- `src/main/java/com/nocobase/web/UnauthorizedException.java` — 标准异常
- `src/main/java/com/nocobase/web/ForbiddenException.java` — 标准异常
- `src/test/java/com/nocobase/ApiCompatibilityTest.java` — 10 个集成测试

**设计说明:**
- 成功响应: `{ "data": ... }` 或 `{ "data": [...], "meta": { "count", "page", "pageSize" } }`
- 错误响应: `{ "errors": [{ "message": "..." }] }`
- 全局异常处理统一返回 NocoBase 兼容格式
- 测试覆盖 6 个核心 API 端点

**测试命令:** `mvn test -Dtest=ApiCompatibilityTest`  
**测试结果:** 10 tests passed  
**是否修改前端文件:** 否

---

### B1: Collection 元数据模型

**主要改动文件:**
- `src/main/java/com/nocobase/entity/CollectionEntity.java` — 完整重写
- `src/main/java/com/nocobase/config/DataInitializer.java` — 更新种子数据
- `src/test/java/com/nocobase/CollectionAndFieldMetadataTest.java` — 6 个测试

**设计说明:**
- 新增字段: `tableName`, `schema`, `template`, `type`, `view`, `sql`, `inherits`, `category`, `hidden`, `system`
- 支持 4 种 collection 类型: physical, view, sql, external
- 系统 collection 标记 `system=true`
- `fields` 表是字段元数据主来源, 不再依赖 `CollectionEntity.fields` (向后兼容保留)
- `getEffectiveTableName()` 返回 `tableName` 或 `name`

**测试命令:** `mvn test -Dtest=CollectionAndFieldMetadataTest`  
**测试结果:** 17 tests passed (含 B1+B2)  
**是否修改前端文件:** 否

---

### B2: Field 元数据模型

**主要改动文件:**
- `src/main/java/com/nocobase/entity/FieldEntity.java` — 完整重写
- `src/test/java/com/nocobase/CollectionAndFieldMetadataTest.java` — 11 个测试

**设计说明:**
- 新增关系字段: `target`, `foreignKey`, `sourceKey`, `targetKey`, `through`, `otherKey`
- 新增选项: `options` (JSON), `system`
- `isRelation()` 判断是否为关系字段
- `generatesPhysicalColumn()` 判断是否生成物理列
  - `belongsTo` + `foreignKey` → 生成外键列
  - `hasOne`, `hasMany`, `belongsToMany` → 不生成当前表列
- `validateFieldName()` 校验字段名, 阻止 SQL 注入

**测试命令:** `mvn test -Dtest=CollectionAndFieldMetadataTest`  
**测试结果:** 17 tests passed (含 B1+B2)  
**是否修改前端文件:** 否

---

### C1: Collection Runtime Registry

**主要改动文件:**
- `src/main/java/com/nocobase/runtime/CollectionDefinition.java` — 不可变集合定义
- `src/main/java/com/nocobase/runtime/FieldDefinition.java` — 不可变字段定义
- `src/main/java/com/nocobase/runtime/RelationDefinition.java` — 不可变关系定义
- `src/main/java/com/nocobase/runtime/CollectionRuntimeService.java` — 运行时注册表
- `src/main/java/com/nocobase/runtime/RuntimeInitializer.java` — 启动加载器

**设计说明:**
- 应用启动时通过 `ApplicationReadyEvent` 自动加载所有 collection
- 使用 `ConcurrentHashMap` 保证线程安全
- `reloadAll()` 和 `reload(collectionName)` 支持运行时刷新
- 不存在的 collection 抛出 `ResourceNotFoundException`
- Controller 和 Repository 必须通过 runtime 获取表名

**测试命令:** `mvn test -Dtest=DataLayerIntegrationTest#runtimeRegistryLoads`  
**测试结果:** passed  
**是否修改前端文件:** 否

---

### C2: Collection Capability 模型

**主要改动文件:**
- `src/main/java/com/nocobase/runtime/CollectionCapability.java`

**设计说明:**
- physical table: `readable=true, writable=true, schemaMutable=true, indexMutable=true, relationSupported=true`
- view collection: `readable=true, writable=false` (只读)
- sql collection: `readable=true, writable=false` (只读)
- external collection: `readable=true, writable=false` (预留)
- `forType(type)` 工厂方法根据类型返回对应能力

**测试命令:** `mvn test -Dtest=DataLayerIntegrationTest#physicalCollectionCapability`  
**测试结果:** passed  
**是否修改前端文件:** 否

---

### E1: DDL Synchronizer

**主要改动文件:**
- `src/main/java/com/nocobase/ddl/DdlPlan.java` — DDL 计划 (含 ColumnDef)
- `src/main/java/com/nocobase/ddl/DialectAdapter.java` — 方言适配器接口
- `src/main/java/com/nocobase/ddl/H2DialectAdapter.java` — H2 实现
- `src/main/java/com/nocobase/ddl/PostgresDialectAdapter.java` — PostgreSQL 实现
- `src/main/java/com/nocobase/ddl/DdlSynchronizer.java` — DDL 同步器

**设计说明:**
- DDL 操作使用 `DdlPlan` 封装, 支持回滚语句
- 方言差异集中在 `DialectAdapter` 实现中
- `DdlSynchronizer` 使用 `@Transactional` 保证元数据和 DDL 原子性
- 支持: CREATE TABLE, ADD COLUMN, DROP COLUMN, CREATE INDEX, DROP INDEX
- 系统字段 (id, created_at, updated_at) 不可删除

**测试命令:** `mvn test -Dtest=DataLayerIntegrationTest#createCollectionWithDdl`  
**测试结果:** passed  
**是否修改前端文件:** 否

---

### E2: 字段类型映射

**主要改动文件:**
- `src/main/java/com/nocobase/field/FieldTypeMapper.java` — 类型注册表
- `src/main/java/com/nocobase/field/FieldTypeDescriptor.java` — 类型描述符

**设计说明:**
- 首批支持 16 种字段类型: string, text, integer, bigInt, float, double, boolean, date, datetime, json, uuid, password, belongsTo, hasOne, hasMany, belongsToMany
- 分类: scalar (标量), relation (关系), virtual (虚拟)
- 关系字段不生成物理列 (belongsTo 除外)

**测试命令:** `mvn test -Dtest=DataLayerIntegrationTest#fieldTypeMapper`  
**测试结果:** passed  
**是否修改前端文件:** 否

---

### D1: DynamicRepository 基础 CRUD

**主要改动文件:**
- `src/main/java/com/nocobase/data/DynamicRepository.java` — 元数据驱动数据访问
- `src/main/java/com/nocobase/controller/GenericCrudController.java` — 重写为委托模式

**设计说明:**
- 替换 `GenericCrudController` 内部直接拼 SQL 的实现
- 所有表名、列名来自 `CollectionRuntime`
- 请求体中未知字段被忽略, 不直接入库
- SQL 参数使用 `?` 绑定, 防止 SQL 注入
- 支持 `list/get/create/update/destroy` 五个操作

**测试命令:** `mvn test -Dtest=DataLayerIntegrationTest#dynamicRepositoryCreate`  
**测试结果:** passed  
**是否修改前端文件:** 否

---

### D2: Filter DSL 编译器

**主要改动文件:**
- `src/main/java/com/nocobase/data/CompiledFilter.java` — 过滤器编译

**设计说明:**
- 支持 13 种操作符: `$eq`, `$ne`, `$gt`, `$gte`, `$lt`, `$lte`, `$in`, `$notIn`, `$and`, `$or`, `$null`, `$notNull`, `$includes`
- 嵌套 `$and/$or` 支持任意深度嵌套
- 所有值使用参数绑定
- 不支持的操作符抛出 `IllegalArgumentException`
- 字段名使用 `"` 引用以兼容大小写敏感

**测试命令:** `mvn test -Dtest=DataLayerIntegrationTest#filterDslBasicOperators`  
**测试结果:** passed  
**是否修改前端文件:** 否

---

### D3: 排序、分页、字段投影

**主要改动文件:**
- (集成在 `DynamicRepository.java` 中)

**设计说明:**
- 分页: `page` (默认1), `pageSize` (默认20)
- 排序: `sort=-id,name` 格式, `-` 表示降序
- 字段投影: `fields=id,name` 限制返回列
- 返回 `meta.count/page/pageSize` 与 NocoBase 兼容
- 投影字段必须来自 metadata

**测试命令:** `mvn test -Dtest=DataLayerIntegrationTest#sortAndPagination`  
**测试结果:** passed  
**是否修改前端文件:** 否

---

### G1: ACL 数据模型

**主要改动文件:**
- `src/main/java/com/nocobase/entity/RoleResource.java` — 角色-资源权限
- `src/main/java/com/nocobase/entity/RoleResourceAction.java` — 角色-资源-动作权限
- `src/main/java/com/nocobase/entity/RoleResourceScope.java` — 角色-资源-数据范围
- `src/main/java/com/nocobase/repository/RoleResourceRepository.java` — Repository

**设计说明:**
- `role` 控制 `resource` 访问
- `action` 控制 `list/get/create/update/destroy` 操作
- `fields` 控制可读/可写字段
- `scope` 保存 filter JSON 用于数据范围权限
- 初始化 `root`、`admin`、`member` 角色 (已在 DataInitializer 中)

**测试命令:** (需配合 ACL 注入测试)  
**测试结果:** 实体和 Repository 编译通过  
**是否修改前端文件:** 否

---

## 三、测试汇总

| 测试类 | 测试数 | 状态 |
|--------|--------|------|
| `NocobaseApplicationTests` | 1 | ✅ 通过 |
| `ApiCompatibilityTest` | 10 | ✅ 通过 |
| `CollectionAndFieldMetadataTest` | 17 | ✅ 通过 |
| `DataLayerIntegrationTest` | 13 | ✅ 通过 |
| **合计** | **41** | **全部通过** |

运行命令:
```bash
mvn test
```

---

## 四、架构总览

```
com.nocobase
├── config/          # SecurityConfig, WebConfig, NocobaseUrlFilter, DataInitializer
├── controller/      # AuthController, GenericCrudController, CollectionController, ...
├── entity/          # JPA 实体: User, Role, CollectionEntity, FieldEntity, ACL entities
├── repository/      # Spring Data JPA Repository 接口
├── security/        # JWT 认证: JwtUtil, JwtAuthenticationFilter
├── runtime/         # 运行时注册表: CollectionRuntimeService, CollectionDefinition, ...
├── ddl/             # DDL 同步: DdlSynchronizer, DialectAdapter, H2DialectAdapter, ...
├── field/           # 字段类型映射: FieldTypeMapper, FieldTypeDescriptor
├── data/            # 数据访问层: DynamicRepository, CompiledFilter
├── web/             # Web 层: ApiResponse, GlobalExceptionHandler, 异常类
└── NocobaseApplication.java
```

---

## 五、已知限制和后续依赖

### 待完善 (Batch 4 剩余):
1. **F1 关系字段查询** — `appends` 机制需要实现 association join 查询
2. **F2 关联动作** — `{resource}.{association}:list/get/add/remove/set` 路由
3. **G2 ACL Query 注入** — `AclService`, `AclFilterInjector`, `CurrentUserContext`
4. **H1 系统模块注册表** — `application_plugins` 完整兼容返回
5. **H2 UI Schema Storage** — `insertAdjacent`, `patch`, `remove` 完整实现

### 关键技术决策:
- 所有标识符使用双引号引用 (`globally_quoted_identifiers=true`)
- 数据库迁移使用 Flyway, 禁用 Hibernate ddl-auto
- 表名/列名必须来自 `CollectionRuntime`, 禁止硬编码
- 所有 SQL 参数使用 `?` 绑定, 防止注入
- 方言差异集中在 `DialectAdapter` 接口

### 暂缓任务 (不在此阶段开发):
- 工作流执行引擎
- 文件管理、通知、备份恢复
- AI、MCP、OAuth/IDP
- npm 插件安装、远程插件市场

---

## 六、文件修改清单

### 新增文件 (38 个):
```
src/main/java/com/nocobase/
├── web/ApiResponse.java
├── web/GlobalExceptionHandler.java
├── web/ResourceNotFoundException.java
├── web/UnauthorizedException.java
├── web/ForbiddenException.java
├── runtime/CollectionDefinition.java
├── runtime/FieldDefinition.java
├── runtime/RelationDefinition.java
├── runtime/CollectionCapability.java
├── runtime/CollectionRuntimeService.java
├── runtime/RuntimeInitializer.java
├── ddl/DdlPlan.java
├── ddl/DialectAdapter.java
├── ddl/H2DialectAdapter.java
├── ddl/PostgresDialectAdapter.java
├── ddl/DdlSynchronizer.java
├── field/FieldTypeMapper.java
├── field/FieldTypeDescriptor.java
├── data/CompiledFilter.java
├── data/DynamicRepository.java
├── entity/RoleResource.java
├── entity/RoleResourceAction.java
├── entity/RoleResourceScope.java
├── repository/RoleResourceRepository.java
└── src/main/resources/db/migration/V1__baseline_schema.sql
└── src/test/java/com/nocobase/
    ├── NocobaseTestBase.java
    ├── NocobaseApplicationTests.java
    ├── ApiCompatibilityTest.java
    ├── CollectionAndFieldMetadataTest.java
    ├── DataLayerIntegrationTest.java
    └── src/test/resources/application.yml
```

### 修改文件 (6 个):
```
src/main/resources/application.yml          — ddl-auto=none, Flyway启用, 引用标识符
src/main/java/com/nocobase/entity/CollectionEntity.java  — 完整字段模型
src/main/java/com/nocobase/entity/FieldEntity.java       — 完整字段模型
src/main/java/com/nocobase/entity/ApplicationPlugin.java — 新增 builtIn
src/main/java/com/nocobase/controller/GenericCrudController.java — 委托DynamicRepository
src/main/java/com/nocobase/config/DataInitializer.java   — system=true, builtIn=true
```

### 是否修改前端文件: **否** (严格遵守约束)